resources:
  - name: aws-terraform-tag-check-pr
    type: pull-request
    source:
      repository: dwp/aws-terraform-tag-check
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h

  - name: aws-terraform-tag-check
    type: git
    source:
      branch: master
      uri: https://github.com/dwp/aws-terraform-tag-check.git
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h
