groups:
  - name: master
    jobs:
      - management-dev
      - management
  - name: pull-request
    jobs:
      - aws-terraform-tag-check-pr
  - name: mirror
    jobs:
      - mirror-dwpdigital-aws-terraform-tag-check-dev
      - mirror-dwpdigital-aws-terraform-tag-check
